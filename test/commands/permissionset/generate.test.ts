/*
import { expect, test } from '@salesforce/command/lib/test';

 describe('permissionset:generate', () => {
  test
    .stdout()
    .command(['permissionset:generate', '-c', '"./Config/generate-permission-sets.json"',
     '-p', '"./Config/packageXml/package_DevRoot_Managed.xml"',
     '-f', '"./Projects/DevRootSource/force-app/main/default"',
     '-o', '"./Projects/DevRootSource/force-app/main/default/permissionsets"'])
    .it('runs permissionset:generate', (ctx) => {
      expect(ctx.stdout).to.contain('');
    });
}); */